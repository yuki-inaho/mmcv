# Local cu12 GPU smoke result (2026-05-28)

This fork branch (`cu121-mmcv2x-gpu-validation-may28`) was used by the parent
sandbox `mmopenlab_cu12_sandbox` to verify cu12 GPU operation on a local box.

| field | value |
| :-- | :-- |
| date (UTC) | 2026-05-28 03:35:51 |
| GPU | NVIDIA RTX 2000 Ada Generation (sm_89, compute capability 8.9) |
| Driver | 550.127.08 (CUDA 12.4 runtime) |
| torch | 2.1.0+cu121 |
| mmcv | 2.1.0 (cu121 wheel: `mmcv-2.1.0-cp310-cp310-manylinux1_x86_64.whl`) |
| CUDA op | `mmcv.ops.box_iou_rotated` |
| op output device | `cuda` |
| op output shape | `[2, 2]`, dtype `torch.float32` |
| result | **success** |

The wheel was fetched from
`https://download.openmmlab.com/mmcv/dist/cu121/torch2.1.0/mmcv-2.1.0-cp310-cp310-manylinux1_x86_64.whl`,
not built from this fork.

## Required change candidates surfaced

This fork main is `mmcv 2.2.0` (`mmcv/version.py`). For cu12 + torch 2.1.0:
- The existing cu121 wheel index covers `mmcv 2.1.0` and `mmcv 2.2.0` for cp310
  torch2.1.0 (no `2.0.x` cu121 wheel exists). Therefore downstream stacks that
  require `mmcv<2.1.0` (e.g., `mmdet 3.0.0rc6`, `mmdet 3.1.0`) cannot move to
  cu121 wheel-only without source build. Either ship `mmcv 2.0.x cu121` wheels
  or upgrade downstream lower bounds.

Evidence (parent repo):
- `06_execution_logs/smoke_mmcv_20260528.json`
- `06_execution_logs/cu12_actual_runtime_validation_20260528_local.json`

Workdoc / acceptance (parent repo):
- `temp/workdoc_May28-2026_cu12_submodule_gpu_validation.md`
- `temp/acceptance_May28-2026_cu12_submodule_gpu_validation.md`
